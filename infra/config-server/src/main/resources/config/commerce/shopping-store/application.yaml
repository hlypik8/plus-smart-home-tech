spring:
  datasource:
    url: "jdbc:postgresql://localhost:6539/store"
    username: store
    password: store
    driver-class-name: "org.postgresql.Driver"
  jpa:
    hibernate:
      ddl-auto: create
      dialect: "org.hibernate.dialect.PostgreSQLDialect"
    show-sql: false
  sql:
    init:
      mode: always