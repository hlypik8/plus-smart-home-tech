cart:

spring:
  datasource:
    url: "jdbc:postgresql://localhost:6538/cart"
    username: cart
    password: cart
    driver-class-name: "org.postgresql.Driver"
  jpa:
    hibernate:
      ddl-auto: create-drop
      dialect: "org.hibernate.dialect.PostgreSQLDialect"
    show-sql: false